server {
    listen 80;
    server_name hub.example.com; # Thay bằng domain thực tế của bạn

    # Docker Registry API (port 5000)
    location / {
        # Cho phép push image dung lượng lớn (không giới hạn)
        client_max_body_size 0;

        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_pass http://localhost:5000;
    }

    # Registry UI (port 5001)
    location /ui {
        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_pass http://localhost:5001/;
    }
}

# Khuyến nghị: Cài SSL bằng Certbot sau khi trỏ domain
# server {
#     listen 443 ssl;
#     server_name hub.example.com;
#     ssl_certificate /etc/letsencrypt/live/hub.example.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/hub.example.com/privkey.pem;
#     ...
# }
